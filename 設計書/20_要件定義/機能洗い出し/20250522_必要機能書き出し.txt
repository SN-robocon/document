●開始用スイッチ制御
	①スイッチによる動作開始制御機能
		・

●ラインオブスタクル
	①探索点まで直進する
		・走行機能
		・カメラ機能(障害物認識)
		・色認識機能(ライントレース)
		・姿勢制御機能(必要であれば)
		
	②障害物を迂回する
		・走行(旋回)機能
		・カメラ機能(障害物認識)
		・色認識機能(ライントレースへの復帰)
		・姿勢制御機能(必要であれば)

●ダブルループ
	①旋回点まで進行する
		・走行(旋回)機能
		・カメラ機能(障害物認識)
		・色認識機能(ライントレース)
		
	②ダブルループに突入する
		・走行(旋回)機能
		・カメラ機能(障害物認識)
		・色認識機能(ライントレース)
		

●スマートキャリー
	①キャリーボトル1
		・走行(旋回)機能
		・カメラ機能(ボトル・置く場所認識)
		・アーム機能(ボトル挟む・離す)
		・色認識機能(ボトルの色認識)
		・
		
	②キャリーボトル2
		・走行(旋回)機能
		・カメラ機能(ボトル・置く場所認識)
		・アーム機能(ボトル挟む・離す)
		・色認識機能(ボトルの色認識)
	
	③ キャリーゲート通過
		・走行(旋回)機能
		・カメラ機能(ゲート認識)
		